@page
@model SMARTLY.Pages.DeleteContactModel
@{
  if(Model.type==1)
    Layout = "/Pages/Shared/_LayoutAdmin.cshtml";
    if(Model.type==4)
     Layout = "/Pages/Shared/_LayoutManager.cshtml";
}
<section id="hero2" class="d-flex align-items-center">
	<div class="container position-relative" data-aos="fade-up" data-aos-delay="500">
		<h1>Delete Contact</h1>

	</div>
</section>
<br />
<br />
<br />
<br />

<div class=" text-center">

<h1>Delete Page</h1>
<p>Are you sure you want to delete this page?</p>
<form method="post">
	<div class="form-group" >
		<button type="submit" class="btn btn-danger" name="continue">Continue</button>
	</div>
   <input hidden asp-for="@Model.username"/>
   <input hidden asp-for="@Model.message" />
	<a asp-page="/Contact_Us_Admin" class="btn btn-secondary">Cancel</a>
</form>
</div>
<br />
<br />
<br />
